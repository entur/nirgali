// Nucleo Icon, see LICENCE.md
// https://nucleoapp.com/license

import React from 'react';
import Proptypes from 'prop-types';
import { getIconSize } from '../helper';

function Success({ backgroundColor = 'currentColor', color = '#FFF', size, ...svgProps }) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 16 16"
      { ...getIconSize(size) }
      { ...svgProps }
    >
      <circle cx="8" cy="8" r="8" fill={ backgroundColor } />
      <path fill={ color } d="M7 11.4L3.6 8 5 6.6l2 2 4-4L12.4 6z" />
    </svg>
  );
}

Success.propTypes = {
  backgroundColor: Proptypes.string,
  color: Proptypes.string,
  size: Proptypes.oneOf(['xlarge', 'large', 'medium', 'small']),
};

export default Success;

// Nucleo Icon, see LICENCE.md
// https://nucleoapp.com/license

import React from 'react';
import Proptypes from 'prop-types';
import { getIconSize } from '../helper';

function Exclamation({ backgroundColor = 'currentColor', color = '#FFF', size, ...svgProps }) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 16 16"
      { ...getIconSize(size) }
      { ...svgProps }
    >
      <circle cx="8" cy="8" r="8" fill={ backgroundColor } />
      <path fill={ color } d="M8 12c-.6 0-1-.4-1-1s.4-1 1-1 1 .4 1 1-.4 1-1 1zm1-3H7V4h2v5z" />
    </svg>
  );
}

Exclamation.propTypes = {
  backgroundColor: Proptypes.string,
  color: Proptypes.string,
  size: Proptypes.oneOf(['xlarge', 'large', 'medium', 'small']),
};

export default Exclamation;

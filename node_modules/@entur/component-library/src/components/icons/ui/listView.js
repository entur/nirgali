// Nucleo Icon, see LICENCE.md
// https://nucleoapp.com/license

import React from 'react';
import PropTypes from 'prop-types';
import { getIconSize } from '../helper';

function ListView({ color = 'currentColor', size, ...svgProps }) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 16 16"
      {...getIconSize(size)}
      {...svgProps}
    >
      <path
        fill={color}
        d="M0,2.99986h12v2H0V2.99986z M14,2.99986h2v2h-2V2.99986z M0,6.99986h12v2.00049H0V6.99986z M14,6.99986h2
		v2.00049h-2V6.99986z M0,11.00035h12v2H0V11.00035z M14,11.00035h2v2h-2V11.00035z"
      />
    </svg>
  );
}

ListView.propTypes = {
  size: PropTypes.oneOf(['xlarge', 'large', 'medium', 'small']),
  color: PropTypes.string,
};

export default ListView;

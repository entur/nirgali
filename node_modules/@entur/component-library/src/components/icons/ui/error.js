// Nucleo Icon, see LICENCE.md
// https://nucleoapp.com/license

import React from 'react';
import Proptypes from 'prop-types';
import { getIconSize } from '../helper';

function Error({ backgroundColor = 'currentColor', color = '#FFF', size, ...svgProps }) {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 16 16"
      { ...getIconSize(size) }
      { ...svgProps }
    >
      <circle cx="8" cy="8" r="8" fill={ backgroundColor } />
      <path fill={ color } d="M11.5 10.1l-1.4 1.4L8 9.4l-2.1 2.1-1.4-1.4L6.6 8 4.5 5.9l1.4-1.4L8 6.6l2.1-2.1 1.4 1.4L9.4 8z" />
    </svg>
  );
}
Error.propTypes = {
  backgroundColor: Proptypes.string,
  color: Proptypes.string,
  size: Proptypes.oneOf(['xlarge', 'large', 'medium', 'small']),
};

export default Error;
